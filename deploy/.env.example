# Скопируй в .env и заполни значениями
# cp deploy/.env.example .env

# Домен (без https://)
DOMAIN=dom-ls.ru

# API URL для фронта и админки (с https://)
VITE_API_URL=https://dom-ls.ru
REACT_APP_API_URL=https://dom-ls.ru

# CORS — разрешённые источники для бэкенда (через запятую)
CORS_ORIGINS=https://dom-ls.ru,https://www.dom-ls.ru

# Секретный ключ JWT (сгенерируй: openssl rand -hex 32)
JWT_SECRET=сгенерируй-длинный-случайный-ключ
